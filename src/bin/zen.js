export default async ({ output }) => {
    await output('Beautiful is better than ugly.')
    await output('Explicit is better than implicit.')
    await output('Simple is better than complex.')
    await output('Complex is better than complicated.')
    await output('Flat is better than nested.')
    await output('Sparse is better than dense.')
    await output('Readability counts.')
    await output("Special cases aren't special enough to break the rules.")
    await output('Although practicality beats purity.')
    await output('Errors should never pass silently.')
    await output('Unless explicitly silenced.')
    await output('In the face of ambiguity, refuse the temptation to guess.')
    await output('There should be one-- and preferably only one--obvious way to do it.')
    await output("Although that way may not be obvious at first unless you're Dutch.")
    await output('Now is better than never.')
    await output('Although never is often better than * right * now.')
    await output("If the implementation is hard to explain, it's a bad idea.")
    await output('If the implementation is easy to explain, it may be a good idea.')
    await output("Namespaces are one honking great idea-- let's do more of those!")
}
